<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dendi.githubusers">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <permission android:name="com.dendi.githubusers.READ_DATABASE" android:protectionLevel="normal"/>
    <permission android:name="com.dendi.githubusers.WRITE_DATABASE" android:protectionLevel="normal"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GithubUsers"
        android:usesCleartextTraffic="true">
        <provider
            android:name=".provider.UserProvider"
            android:authorities="com.dendi.githubusers"
            android:enabled="true"
            android:exported="true"
            android:readPermission="com.dendi.githubusers.READ_DATABASE"
            android:writePermission="com.dendi.githubusers.WRITE_DATABASE"/>

        <receiver
            android:name=".helper.AlarmReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".view.SettingActivity" />
        <activity android:name=".view.FavoriteActivity" />
        <activity
            android:name=".view.SplashScreen"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.DetailUser" />
        <activity android:name=".view.MainActivity" />
    </application>

</manifest>